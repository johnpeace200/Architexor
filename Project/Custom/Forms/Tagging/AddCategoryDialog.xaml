<Window x:Class="Architexor.Forms.AddCategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Tag Category"
        Width="250" Height="150"
				FontFamily="Arial"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
				Background="#F0F0F0"
				>
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<StackPanel Orientation="Vertical" Grid.Row="0">
			<TextBlock Text="Select a category:" FontSize="14" Margin="0,5,0,10" />
			<ComboBox x:Name="cmbCategory" Width="200" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,10,0,0">
			<Button x:Name="btnOK" Content="OK" Width="70" Height="25"  Margin="0,0,10,0" Click="btnOK_Click"/>
			<Button Content="Cancel" Width="70" Height="25" IsCancel="True"/>
		</StackPanel>
	</Grid>
</Window>

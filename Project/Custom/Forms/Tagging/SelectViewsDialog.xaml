<Window x:Class="Architexor.Forms.SelectViewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Views"
        Width="400" Height="420"
				FontFamily="Arial"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
				Background="#F0F0F0"
				>
	<Window.Resources>
		<Style TargetType="ListBoxItem" x:Key="NoHighlightListBoxItemStyle">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
			<Setter Property="Padding" Value="2"/>
			<Style.Triggers>
				<Trigger Property="IsSelected" Value="True">
					<Setter Property="Background" Value="Transparent"/>
				</Trigger>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="Transparent"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/> <!-- Title -->
			<RowDefinition Height="*"/> <!-- List-->
			<RowDefinition Height="Auto"/> <!-- Buttons -->
		</Grid.RowDefinitions>

		<!-- Header Label -->
		<TextBlock Text="Select views:" FontSize="14" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,5"/>
		
		<!-- View List with CheckBoxes -->
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<ListBox x:Name="lstViews" Grid.Column="0" SelectionMode="Multiple" ItemContainerStyle="{StaticResource NoHighlightListBoxItemStyle}">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<CheckBox Content="{Binding}" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}"/>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
			<!-- Check All / None Buttons-->
			<StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0" VerticalAlignment="Top">
				<Button x:Name="btnCheckAll" Content="Check All" Width="90" Height="25" Margin="0,0,0,5" Click="btnCheckAll_Click"/>
				<Button x:Name="btnCheckNone" Content="Check None" Width="90" Height="25" Click="btnCheckNone_Click"/>
			</StackPanel>
		</Grid>
		
		<!-- OK /Cancel Buttons-->
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
			<Button x:Name="btnOK" Content="OK" Width="70" Height="25"  Margin="0,0,10,0" Click="btnOK_Click"/>
			<Button Content="Cancel" Width="70" Height="25" IsCancel="True"/>
		</StackPanel>
	</Grid>
</Window>
